# Завдання 2: Візуалізація фрактального дерева

Це завдання присвячене вивченню рекурсії через графічне представлення фракталів. Реалізовано модель, що імітує структуру природного дерева.

---

## Опис алгоритму

Алгоритм базується на принципі самоподібності: кожна гілка дерева є меншою копією стовбура, що розгалужується під певним кутом.

### Ключові особливості реалізації

* **Рекурсивна глибина:** Користувач може самостійно вказати рівень рекурсії через діалогове вікно. Це дозволяє дослідити, як складність фрактала зростає експоненціально.
* **Динамічна товщина:** Товщина ліній (`pensize`) зменшується з кожним рівнем рекурсії. Це створює візуальний ефект масивного стовбура та тонких кінцевих гілочок.
* **Кольорова схема:** Реалізовано перехід кольорів від коричневого (стовбур та скелетні гілки) до зеленого (тонкі гілки та листя), що робить зображення реалістичним.
* **Геометрія:** Гілки зменшуються на коефіцієнт $0.75$ на кожному кроці, а кут розгалуження становить $30^\circ$.

---

## Аналіз складності

* **Часова складність:** $O(2^n)$, де $n$ — рівень рекурсії. Оскільки кожна гілка породжує дві нові, кількість операцій зростає як степінь двійки.
* **Просторова складність:** $O(n)$ для стека викликів рекурсії.

---

## Використання

1. **Запустіть файл:**

    ```bash
    python pythagoras_tree.py
    ```

2. **Введіть рівень рекурсії:** У діалоговому вікні, що з’явиться, вкажіть глибину (рекомендовано **7–10** для оптимального балансу деталізації та швидкості).
3. **Спостерігайте за процесом:** Програма почне малювання. Закрийте вікно після завершення, щоб повернутися до головного меню.

---

## Технічні деталі реалізації

* **Рекурсія:** Глибина дерева регулюється рекурсивними викликами, де довжина гілок зменшується на кожному рівні за певним коефіцієнтом.
* **Колірний градієнт:** Функція `get_color_intensity` динамічно розраховує колір ліній, забезпечуючи плавний перехід від коричневого (**стовбур**) до яскраво-зеленого (**листя**).
* **Оптимізація:** Використано метод `screen.tracer(0)` та `screen.update()`. Це дозволяє виконувати обчислення у фоновому режимі та миттєво відображати готовий складний фрактал без затримок анімації малювання кожної лінії.

---

## Висновки

Завдання наочно ілюструє потужність рекурсивних алгоритмів для створення складних самоподібних геометричних структур (**фракталів**). Програмна реалізація підтверджує, що прості математичні правила можуть створювати складні образи, які імітують біологічні форми.

---
